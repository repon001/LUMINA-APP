# Normalise to LF in the repo and in the working tree. Without this, Windows
# checkouts rewrite every file to CRLF and git warns on each commit.
* text=auto eol=lf

# Generated, and treated as binary for diffs.
package-lock.json -diff linguist-generated
